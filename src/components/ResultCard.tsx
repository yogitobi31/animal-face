import type { AnimalCard } from '../types/animal'
export default function ResultCard({card,score}:{card:AnimalCard;score:number}){const [err,setErr]=useState(false);return <div id='result-card' className='rounded-3xl bg-gradient-to-br from-amber-50 to-stone-200 p-5 shadow-sm overflow-hidden'><div className='text-sm text-stone-500'>당신의 동물상 카드는</div><h2 className='text-2xl font-semibold mt-1'>{card.name}</h2><p className='text-sm text-stone-600'>{card.blend.join(' × ')}</p><div className='mt-4 h-56 rounded-2xl bg-stone-300/40 overflow-hidden'>{!err?<img src={card.imageUrl} className='h-full w-full object-cover' onError={()=>setErr(true)}/>:<div className='h-full flex items-center justify-center text-stone-600'>Premium Card Preview</div>}</div><p className='mt-4 text-sm'>{card.catchphrase}</p><p className='text-sm text-stone-600 mt-2'>{card.description}</p><p className='mt-2 text-sm'>매칭률 {score}%</p><p className='mt-2 text-xs text-stone-500'>Animal Face Archive</p></div>}
import { useState } from 'react'
